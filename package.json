{
  "name": "calculator-app-validation",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  },
  "engines": { "node": ">=24.20.0" }
}
